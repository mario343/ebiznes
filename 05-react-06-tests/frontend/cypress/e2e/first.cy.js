//first test
describe("My First Test", () => {
  it("tells whether true is actually true!", () => {
    expect(true).to.equal(true);
  });
  it("tells whether true is actually true!", () => {
    expect(true).to.equal(true);
  });
  it("math is correct + addition", () => {
    expect(2 + 2).to.equal(4);
  });
  it("math is correct - subtracting", () => {
    expect(2 - 1).to.equal(1);
  });
});
